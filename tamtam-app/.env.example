# Supabase project URL — auth only (login/register/session via supabase-js)
EXPO_PUBLIC_SUPABASE_URL=
# Supabase anon key — auth only
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Web app base URL — all data goes through its /api/echo/* routes (lib/api.ts)
EXPO_PUBLIC_API_URL=https://www.tamma.me
